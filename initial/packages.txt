# install yay
sudo pacman -S --needed git base-devel && git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si

# pacman
cliphist                # clipboard manager to use with wofi
wtype                   # key presser for paste when selecting wofi
zsh-syntax-highlighting # with catppuccin
hyprlock                # lock screen
waybar                  # topbar
wofi                    # app search
kitty                   # terminal
xclip
zsh                     # bash
neovim                  # text editor
