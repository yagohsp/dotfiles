;; ========================================
;; Variables
;; ========================================

(defvar show_panel false)

(defvar volume_osd_value 0)
(defvar volume_osd_muted false)

(defpoll time 
    :interval "1s"
    "date '+%H:%M'")

(defpoll date 
    :interval "60s"
    "date '+%a %d %b'")

(deflisten workspaces 
    :initial "[]"
    "scripts/workspaces-all.sh")

(deflisten output_device_volumes
    :initial "[]"
    "scripts/listen-output-device-volumes.sh")

(deflisten app_stream_routes
    :initial "[]"
    "scripts/listen-stream-routes.sh")

(deflisten default_sink_options
    :initial "[]"
    "scripts/listen-default-sink-options.sh")

;; ========================================
;; Widgets
;; ========================================

(include "widgets/workspaces.yuck")
(include "widgets/time.yuck")
(include "widgets/volume-toggle.yuck")
(include "widgets/tray-system.yuck")
(include "widgets/panel-toggle.yuck")
(include "widgets/bar.yuck")

;; ========================================
;; Windows
;; ========================================

(include "windows/bar.yuck")
(include "windows/calendar-dropdown.yuck")
(include "windows/volume-modal.yuck")
(include "windows/system-menu.yuck")
(include "windows/panel.yuck")
(include "windows/volume-osd.yuck")
